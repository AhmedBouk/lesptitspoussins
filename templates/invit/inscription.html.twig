{% extends 'base.html.twig' %}

{% block body %}

    {{ parent() }}
    <div class="container">
        <div class="row">
            <div class="col-md-7 offset-md-3">
            <div class="form-group">
                <div class="">
                    <h1 class="h1 offset-md-3 mt-3">Inscription</h1>
                    <div class="form-group mb-3">
                    {{ form_start(form) }}
                            {{form_row(form.mail)}}
                        </div>
                            {{form_row(form.password.first)}}
                        </div>
                            {{form_row(form.password.second)}}
                        </div>
                    </div>
                    <div class="form-check m-3">
                        {{ form_row(form.Conditionsdutilisations) }}<a href="">Pour les lire c'est ici</a>
                    </div>
            <input type="hidden" name="token" value="{{ csrf_token('create-parentt') }}">
                    <input type="submit"  value="Inscription" class="btn btn-success   offset-md-4">
                    {{ form_end(form) }}
                </div>
            </div>
            </div>
        </div>
    </div>
{% endblock body %}

